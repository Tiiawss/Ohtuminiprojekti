[tool.poetry]
name = "production"
version = "0.1.0"
description = ""
authors = ["Tuomas Marenk <tuomas.marenk@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
gunicorn = "^20.1.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
